# xhaip ConfigMap — non-sensitive configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: xhaip-config
  namespace: xhaip
data:
  HAIP_ENV: "production"
  LOG_LEVEL: "INFO"
  JSON_LOGS: "true"
  AUTH_ENABLED: "true"
  AUDIT_ENABLED: "true"
